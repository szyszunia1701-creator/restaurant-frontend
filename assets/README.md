# Branding assets

Place the restaurant assistant logo at `chatbot-logo.png`, or update the single
`RESTAURANT_CONFIG.branding.chatbotLogo` path in `restaurant-config.js`.
The interface displays a neutral fallback icon if the configured image is unavailable.
